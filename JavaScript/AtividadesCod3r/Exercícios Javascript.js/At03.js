// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

function potenciacao (base, expoente) {
   calculo = Math.pow(base, expoente)
   return calculo
}

console.log(potenciacao(2, 0.5))